# Project Radar Agent R3 — stage inference + join

```bash
npm install
npm run dev
```

- Health: `GET http://localhost:8013/health`
- Docs: `http://localhost:8013/docs`
- Score: `POST /score`
- Join: `POST /join`
- Projects: `GET /projects`
