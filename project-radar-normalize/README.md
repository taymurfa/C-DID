# Project Radar Agent R2 — normalize + entity resolution

```bash
npm install
npm run dev
```

- Health: `GET http://localhost:8012/health`
- Docs: `http://localhost:8012/docs`
- Normalize: `POST /normalize` with raw `records` from R1
